.env
__pycache__/
*.pyc
.DS_Store
*.csv
data/
venv/
.venv/

# polling output, grows without bound (80MB and counting)
poll_log.txt

# node deps live in frontend/ only. This root entry is a backstop: a stray
# `npm install` here once committed 2,443 files before anyone noticed.
node_modules/
