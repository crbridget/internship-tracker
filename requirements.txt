# Pull torch from PyTorch's CPU index. On Linux, plain `pip install torch`
# resolves to the CUDA build (~2.5GB) and will exceed the build limits on most
# hosts; the +cpu wheel is ~200MB. macOS has no CUDA builds, so the marker below
# keeps `pip install -r requirements.txt` working locally too.
--extra-index-url https://download.pytorch.org/whl/cpu

Flask==3.1.3
flask-cors==6.0.5
gunicorn==23.0.0
python-dotenv==1.2.2
requests==2.34.2
supabase==2.31.0

# scoring
sentence-transformers==5.7.0
scikit-learn==1.7.2
torch==2.13.0+cpu ; sys_platform == "linux"
torch==2.13.0 ; sys_platform != "linux"
